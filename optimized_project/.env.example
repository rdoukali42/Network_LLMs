# Example .env file for optimized_project
# Copy this to .env and fill in your actual API keys.
# These will be loaded by optimized_project/config/core_config.py

# Google Gemini API Key (for LLM tasks)
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# Google Cloud API Keys (if using Google Cloud for STT/TTS in voice_service.py)
# These are examples; actual variable names might depend on how you implement voice_service
# Ensure the service account JSON path is correctly set if using that method,
# or that the API keys below are for services enabled on your Google Cloud project.

# If using API keys directly for Google Cloud TTS/STT (less common, usually service account JSON):
# GOOGLE_CLOUD_TTS_API_KEY="YOUR_GOOGLE_CLOUD_TTS_API_KEY"
# GOOGLE_CLOUD_STT_API_KEY="YOUR_GOOGLE_CLOUD_STT_API_KEY"

# If using a service account JSON file for Google Cloud services:
# You might not need separate keys if the service account file is picked up by Google client libraries.
# Ensure GOOGLE_APPLICATION_CREDENTIALS environment variable is set to the path of your service account JSON file,
# or place the JSON file in a location where the Google Cloud client libraries can automatically find it.
# Example: GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account-file.json"

# Langfuse details (Optional - for observability)
# LANGFUSE_SECRET_KEY="YOUR_LANGFUSE_SECRET_KEY"
# LANGFUSE_PUBLIC_KEY="YOUR_LANGFUSE_PUBLIC_KEY"
# LANGFUSE_HOST="https://cloud.langfuse.com" # or your self-hosted instance

# Other potential environment variables
# OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE" # If you were to add an OpenAI agent/tool
# TAVILY_API_KEY="YOUR_TAVILY_API_KEY_HERE" # If using Tavily search tool

# Database path (example if you want to make it configurable via env)
# DATABASE_PATH="data/databases/employees.db" # Relative to optimized_project root
PROJECT_ROOT_OPTIMIZED="." # This helps core_config.py determine its base if needed dynamically
# This is usually determined by Path(__file__).resolve().parent.parent in core_config.py

# Streamlit specific (usually not needed in .env unless for build/deploy)
# STREAMLIT_SERVER_PORT=8501
